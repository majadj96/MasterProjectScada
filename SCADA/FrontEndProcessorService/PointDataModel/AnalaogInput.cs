using ScadaCommon;
using ScadaCommon.Interfaces;

namespace FrontEndProcessorService.PointDataModel
{
    internal class AnalaogInput : AnalogBase
	{
		public AnalaogInput(IConfigItem c, int i)
			: base(c, i)
		{
		}
    }
}