<Window x:Class="UserInterface.AnalyticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserInterface"
        xmlns:views="clr-namespace:UserInterface.View"
        xmlns:analytics="clr-namespace:UserInterface.View.AnalyticsViews"
        xmlns:viewModel="clr-namespace:UserInterface.ViewModel"
        xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Analytics Window" Height="700" Width="1000" Background="LightGray" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:AnalyticsViewModel}">
            <views:TableView/>
        </DataTemplate>

        <Style x:Key="MyButtonStyle" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border" 
                            BorderThickness="0"
                            Padding="4,2" 
                            BorderBrush="DarkGray" 
                            CornerRadius="3" 
                            Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FFF7CE12" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF474747">

        <StackPanel x:Name="SubstationsPanel" Canvas.Left="3" Margin="40,297,808,352" Orientation="Vertical"/>
        <wpf:CartesianChart Margin="254,152,22,48" Series="{Binding SeriesCollection}" Foreground="{x:Null}" Background="White">
			<wpf:CartesianChart.AxisX>
				<wpf:Axis LabelFormatter="{Binding Formatter}" MinValue="{Binding InitialDateTime.Ticks}" MaxValue="{Binding MaxDateTime.Ticks}">
				</wpf:Axis>
			</wpf:CartesianChart.AxisX>
			<wpf:CartesianChart.DataTooltip>
				<wpf:DefaultTooltip SelectionMode="OnlySender" Foreground="Black" />
			</wpf:CartesianChart.DataTooltip>
		</wpf:CartesianChart>
        <ComboBox HorizontalAlignment="Left" ItemsSource="{Binding SubstationList}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedSubstation, Mode=TwoWay}" Margin="118,189,0,0" VerticalAlignment="Top" Height="24" Width="120" FontFamily="Consolas"/>
        <Label Content="Subsation:" HorizontalAlignment="Left" HorizontalContentAlignment="Left" Margin="26,188,0,0" VerticalAlignment="Top" FontFamily="Consolas" Width="81" Foreground="White" FontSize="13"/>
        <Label Content="Measurements" HorizontalAlignment="Left" Margin="54,280,0,0"  HorizontalContentAlignment="Center" VerticalAlignment="Top" FontFamily="Consolas" Width="144" Foreground="White" FontSize="13"/>
        <ListView HorizontalAlignment="Left" ItemsSource="{Binding SignalList}" Height="298" Margin="26,317,0,0" VerticalAlignment="Top" Width="199" Background="#FFA2A2A2" BorderBrush="Black" BorderThickness="2" FontSize="13">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <analytics:SignalListItem/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Label Content="Analytic" HorizontalAlignment="Left" HorizontalContentAlignment="Center" Margin="10,24,0,0" VerticalAlignment="Top" FontFamily="Consolas" Width="972" Foreground="White" FontSize="30" Height="57"/>
        <DatePicker HorizontalAlignment="Left" Margin="123,135,0,0" VerticalAlignment="Top" SelectedDate="{Binding EndDate}"/>
        <DatePicker HorizontalAlignment="Left" Margin="123,85,0,0" VerticalAlignment="Top" SelectedDate="{Binding StartDate}"/>
        <Label Content="Start date:" HorizontalAlignment="Left" HorizontalContentAlignment="Left" Margin="26,85,0,0" VerticalAlignment="Top" FontFamily="Consolas" Width="92" Foreground="White" FontSize="13"/>
        <Label Content="End date:" HorizontalAlignment="Left" HorizontalContentAlignment="Left" Margin="26,135,0,0" VerticalAlignment="Top" FontFamily="Consolas" Width="92" Foreground="White" FontSize="13"/>

    </Grid>
</Window>
